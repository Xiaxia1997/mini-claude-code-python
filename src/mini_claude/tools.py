"""Chapter 2 workspace for the hand-written tool system.

The tool schema and execution loop are intentionally added while following
Chapter 2 instead of being pre-filled in the initial repository version.
"""
